'use strict'
define(
//[        'jquery', 'underscore', 'backbone'],
//function( $,        _,            Backbone ) {
[        'backbone'],
function( Backbone ) {
	return {
		foo: 'foo',
		bar: 'bar'
	}
});

/*var Moon.Images = ; */
